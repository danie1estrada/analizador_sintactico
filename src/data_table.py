class DataTable:

    def __init__(self, headers, dataset):
        self.__dataset = dataset
        pass

    def __create(self):
        
        column_widths = []
        for i in range(len(self.__dataset[0].keys())):
            pass

        index = 0
        for key in self.__dataset[0].keys():
            for i in range(len(self._dataset)):

                pass